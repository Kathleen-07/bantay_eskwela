/// Non-web stub. On mobile/desktop the caller uses url_launcher instead,
/// so this should never actually run, but it must exist for compilation.
Future<bool> openUrlWeb(String url) async => false;
